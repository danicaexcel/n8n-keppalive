# In your render.yaml
services:
  - type: cron
    name: n8n-keepalive
    runtime: image
    plan: free
    schedule: "*/5 * * * *"
    startCommand: "bash keepalive.sh"
    buildCommand: ""  # Explicitly set empty build command
